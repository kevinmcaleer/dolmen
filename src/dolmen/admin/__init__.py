"""The build front end.

A browser UI for authoring the site: a file tree, a Monaco editor, a live
preview and drag-and-drop images. It is served only by `dolmen serve`, never
written into the output — the published site stays a folder of static files.
"""
